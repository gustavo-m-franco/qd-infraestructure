/.idea
# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which might contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data-specific to the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used for local overrides.
*.override.tf
*.override.tf.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore any .plan files, which contain execution plans and may contain secrets
*.plan

# Ignore any .env files used for environment variables in local development
.env
